
#include <iostream>
using namespace std;
int main()
{
    int arr[1000];
    int n;
    cout << "Enter the size of array: ";
    cin >> n;
    // values in an array
    cout << "Enter values in an array: " << endl;
    for (int i = 0; i < n; i++)
        cin >> arr[i];

    // implimenting insertion sort

    return 0;
}
